# TODO: 後でtemplate.yamlをprod用に書き換える
# 動作としては設定するのではなく、あくまでtemplate.ymlに定義されたものを上書きするように動く
# しかも--env-varsオプションはsam invokeとsam start-apiでしか使えないので、こちらのjsonをdev用にtemplate.yamlはprod用にするのがよさそう。
{
  "ShortenFunction": {
    "BASE_URL": "https://shortener.com",
    "AWS_REGION": "ap-northeast-1",
    "DYNAMO_ENDPOINT": "http://dynamodb-local:8000",
    "DYNAMO_TABLE": "dev_urls"
  }
}
