platform: linux

image_resource:
  type: docker-image
  source: { repository: python, tag: alpine }

inputs:
- name: sus

run:
  path: sus/ci/deploy.sh

params:
  AWS_ACCESS_KEY_ID: ((aws_access_key_id))
  AWS_SECRET_ACCESS_KEY: ((aws_secret_access_key))
